@model WebBanThatLung.Models.MauModel

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Areas/NhanVien/Views/Shared/_LayoutNhanVien.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header text-white">
                    <h4 class="card-title mb-0">Thương hiệu</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="ID_MAU" />
                        <div class="form-group mb-3">
                            <label asp-for="MAU" class="control-label">Tên thương hiệu</label>
                            <input asp-for="MAU" class="form-control" />
                            <span asp-validation-for="MAU" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <input type="submit" value="Lưu" class="btn btn-primary" style="background-color:#212529;" />
                            <a asp-action="TrangMau" class="btn btn-secondary ms-2">Quay lại danh sách</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

